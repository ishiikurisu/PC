#include <iostream>
#include <string>
#include <list>
using namespace std;

main()
{
  int numsen;
  list<string> sentences;

  cin >> numsen;
  while (numsen > 0)
  {
    for (int s = 0; s < numsen; ++s)
    {
      string sentence;
      cin >> sentence;
    }
  }
}
